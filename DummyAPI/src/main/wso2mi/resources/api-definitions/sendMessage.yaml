openapi: 3.0.1
info:
  title: sendMessage
  description: API Definition of sendMessage
  version: 1.0.0
servers:
- url: http://localhost:8290/sendmessage
paths:
  /v1:
    post:
      requestBody:
        description: Sample Payload
        content:
          application/json:
            schema:
              type: object
              properties:
                payload:
                  type: object
        required: false
      responses:
        default:
          description: Default response
